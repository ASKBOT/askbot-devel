language: python
python:
  - "2.7"
install:
  - pip install -q -r askbot_requirements_dev.txt
  - python setup.py develop
before_script:
  - askbot-setup -n . -e2 -d data.db
  - python manage.py syncdb --migrate
script:
  - python manage.py test askbot
